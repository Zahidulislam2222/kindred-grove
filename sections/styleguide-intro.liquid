{%- comment -%}
  Styleguide — Intro. Title and orientation for the component library page.
{%- endcomment -%}

<section class="section-tight">
  <div class="container stack">
    <p class="eyebrow">Living documentation</p>
    <h1 class="h1">Kindred Grove Styleguide</h1>
    <p class="text-lg text-muted">Every token, every component, every state. Grows with the theme — if it ships to the store, it ships here first.</p>
  </div>
</section>

{% schema %}
{
  "name": "Styleguide — Intro",
  "class": "section-styleguide-intro",
  "settings": []
}
{% endschema %}
