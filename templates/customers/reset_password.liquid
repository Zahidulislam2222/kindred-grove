{% if settings.demo_mode %}
  <section class="kg-account section"><div class="container"><div class="kg-account__panel">{% render 'demo-disabled-message', message_key: 'demo.personal_flows_disabled' %}</div></div></section>
{% else %}
<section class="kg-account section">
  <div class="container kg-account__shell stack">
    <header class="stack-sm text-center">
      <p class="eyebrow">{{ 'customer.account.breadcrumb' | t }}</p>
      <h1 class="h1">{{ 'customer.reset_password.title' | t }}</h1>
      <p class="text-muted">{{ 'customer.reset_password.subtext' | t }}</p>
    </header>

    <div class="kg-account__panel">
      {%- form 'reset_customer_password', class: 'stack-sm' -%}
        {%- if form.errors -%}
          <ul class="kg-wholesale-form__errors" role="alert">
            {%- for field in form.errors -%}
              <li>{{ form.errors.messages[field] | escape }}</li>
            {%- endfor -%}
          </ul>
        {%- endif -%}
        {% assign field_label_customer_reset_password_password = 'customer.reset_password.password' | t %}
{% render 'form-field', id: 'ResetPassword', type: 'password', name: 'customer[password]', label: field_label_customer_reset_password_password, required: true, autocomplete: 'new-password' %}
        {% assign field_label_customer_reset_password_confirm = 'customer.reset_password.confirm' | t %}
{% render 'form-field', id: 'ResetPasswordConfirm', type: 'password', name: 'customer[password_confirmation]', label: field_label_customer_reset_password_confirm, required: true, autocomplete: 'new-password' %}
        <button type="submit" class="button button--primary">{{ 'customer.reset_password.submit' | t }}</button>
      {%- endform -%}
    </div>
  </div>
</section>
{% endif %}
