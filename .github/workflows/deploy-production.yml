name: Production release verification (writes disabled)

on:
  workflow_dispatch:
    inputs:
      target:
        description: Explicit target for a future reviewed release
        required: true
        type: choice
        options: [production-live]

permissions:
  contents: read

concurrency:
  group: production-release-verification
  cancel-in-progress: false

jobs:
  verify:
    name: Production verification (no live write)
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false

      - name: Read pinned toolchain
        id: toolchain
        run: node scripts/ci/toolchain.cjs >> "$GITHUB_OUTPUT"

      - name: Setup Node.js
        uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7.0.0
        with:
          node-version: ${{ steps.toolchain.outputs.node }}

      - name: Download and verify Gitleaks
        env:
          GITLEAKS_VERSION: ${{ steps.toolchain.outputs.gitleaks_version }}
          GITLEAKS_SHA256: ${{ steps.toolchain.outputs.gitleaks_sha256 }}
        run: |
          set -euo pipefail
          archive="$RUNNER_TEMP/gitleaks.tar.gz"
          curl --fail --silent --show-error --location \
            "https://github.com/gitleaks/gitleaks/releases/download/v${GITLEAKS_VERSION}/gitleaks_${GITLEAKS_VERSION}_linux_x64.tar.gz" \
            --output "$archive"
          printf '%s  %s\n' "$GITLEAKS_SHA256" "$archive" | sha256sum --check --status
          tar -xzf "$archive" -C "$RUNNER_TEMP" gitleaks

      - name: Run offline regression tests
        env:
          GITLEAKS_BIN: ${{ runner.temp }}/gitleaks
        run: node --test tests/security/*.test.cjs

      - name: Install pinned Shopify CLI
        env:
          SHOPIFY_CLI_VERSION: ${{ steps.toolchain.outputs.shopify_cli }}
        run: npm install --global "@shopify/cli@${SHOPIFY_CLI_VERSION}" --no-audit --no-fund

      - name: Run theme-check
        run: shopify theme check --fail-level=error

      - name: Block live write pending reviewed drift and parity release gate
        run: node scripts/ci/block-theme-deploy.cjs
