name: Deploy — Dev theme

on:
  push:
    branches: ['dev/**']
    paths-ignore:
      - 'docs/**'
      - '**.md'
      - '.gitignore'
      - '.editorconfig'
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: deploy-dev-${{ github.ref }}
  cancel-in-progress: true

jobs:
  deploy:
    name: Push to dev theme
    runs-on: ubuntu-latest
    timeout-minutes: 10
    environment: dev

    steps:
      - name: Preflight — check deploy secrets
        id: preflight
        env:
          TOKEN: ${{ secrets.SHOPIFY_CLI_THEME_TOKEN }}
          STORE: ${{ secrets.SHOPIFY_STORE }}
          THEME: ${{ secrets.SHOPIFY_THEME_ID_DEV }}
        run: |
          if [ -z "$TOKEN" ] || [ -z "$STORE" ] || [ -z "$THEME" ]; then
            {
              echo "### Deploy skipped — secrets not provisioned"
              echo ""
              echo "One or more of SHOPIFY_CLI_THEME_TOKEN / SHOPIFY_STORE / SHOPIFY_THEME_ID_DEV is empty."
              echo ""
              echo "See \`docs/CI-SECRETS.md\` for mint steps."
            } >> "$GITHUB_STEP_SUMMARY"
            echo "::warning::Dev deploy skipped — required secrets missing."
            echo "skip=true" >> "$GITHUB_OUTPUT"
          else
            echo "skip=false" >> "$GITHUB_OUTPUT"
          fi

      - name: Checkout
        if: steps.preflight.outputs.skip != 'true'
        uses: actions/checkout@v4

      - name: Setup Node.js
        if: steps.preflight.outputs.skip != 'true'
        uses: actions/setup-node@v4
        with:
          node-version: '22'

      - name: Install Shopify CLI
        if: steps.preflight.outputs.skip != 'true'
        run: npm install -g @shopify/cli@latest

      - name: Theme check (fail fast)
        if: steps.preflight.outputs.skip != 'true'
        run: shopify theme check --fail-level=error

      - name: Push to dev theme
        if: steps.preflight.outputs.skip != 'true'
        env:
          SHOPIFY_CLI_THEME_TOKEN: ${{ secrets.SHOPIFY_CLI_THEME_TOKEN }}
          SHOPIFY_FLAG_STORE: ${{ secrets.SHOPIFY_STORE }}
          SHOPIFY_FLAG_FORCE: '1'
        run: |
          shopify theme push \
            --json \
            --theme ${{ secrets.SHOPIFY_THEME_ID_DEV }} \
            --store "$SHOPIFY_FLAG_STORE" \
            --password "$SHOPIFY_CLI_THEME_TOKEN"
