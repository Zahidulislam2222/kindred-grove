/* Theme entry point. Optional telemetry is disabled in the maintained privacy configuration. */
(function () {
  'use strict';
  // Theme features are initialized by their own assets. No analytics, RUM, or error collector loads here.
})();
